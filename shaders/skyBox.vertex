attribute vec3 aVertexPosition;
attribute vec2 aTexCoord;

uniform mat4 camMatrix;
uniform mat4 frustum;

varying vec2 vTexCoord;

 void main(void) {
	vTexCoord = aTexCoord;
	gl_Position = frustum * camMatrix  * vec4(aVertexPosition, 1.0);
}
